/*$( ".tt" ).click(function() {
	alert('iu');
  //$( "#toggle-frm" ).toggle( "slow", function() {});
});

/*$(window).resize(){
	setElementHeightFromBodyHeight('body');

	function setElementHeightFromBodyHeight(className){
		var headerHeight = $('.header').height();
		var footerHeight = $('.footer').height();
		var windowHeight = $(window).height();
		var bodyHeight = windowHeight - (headerHeight + footerHeight);
		alert(footerHeight);
		$('.'+className).css('height',bodyHeight+'px');
	}
});*/

/*$(window).resize(function(){
	setElementHeightFromBodyHeight('body');
	setElementHeightFromBodyHeight('block-menu');
	setElementHeightFromBodyHeight('block-body');
	setElementHeightFromBodyHeight('toggle-div');
	setElementHeightFromBodyHeight('wrapper');

	function setElementHeightFromBodyHeight(className){
		var headerHeight = $('.header').height();
		var footerHeight = $('.footer').height();
		var windowHeight = $(window).height();
		var bodyHeight = windowHeight - (headerHeight + footerHeight);
		$('.'+className).css('height',bodyHeight+'px');
	}
});*/